/**
 * AdditionService class
 */
class AdditionService {
    /**
     * Add two numbers
     * @param {number} a
     * @param {number} b
     * @returns {number}
     */
    add(denominator, numerator) {
        return denominator + numerator;
    }
}